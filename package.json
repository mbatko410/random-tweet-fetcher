{
  "name": "random-tweet-fetcher",
  "version": "1.0.0",
  "description": "Fetch random tweets from specific accounts.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.17.1",
    "node-fetch": "^3.2.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
